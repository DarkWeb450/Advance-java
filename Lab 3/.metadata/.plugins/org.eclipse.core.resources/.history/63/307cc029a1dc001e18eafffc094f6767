package Lab3.Hibernate;

import java.util.List;
import org.hibernate.Session;
import org.hibernate.Transaction;



public class Select 
{
	public static void main(String[] args) 
	{
		Session session = HibernateUtil.getSessionFactory().openSession();
        
        Transaction tx = session.beginTransaction();
		List<hospital> Hospotal = session.createQuery("FROM hospital", hospital.class).getResultList();
	    if(hospital.isEmpty()) 
	    {
	        System.out.println("No data found.");
	    } 
	    else 
	    {
	        System.out.println("ID\tName\t\t\tAge\tDate Of Birth");
	        for(hospital hospital : Hospotal) {
	            System.out.println(hospital.getID() + "\t" + hospital.getFirst_Name() + " " + hospital.getLast_Name() + "\t\t" + hospital.getAge() + "\t" + hospital.getDOB());
	        }
	    }

        tx.commit();
        session.close();
	}
}
